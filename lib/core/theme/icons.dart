import 'package:flutter/material.dart';

sealed class AppIcons {
  static const back = Icon(Icons.arrow_circle_up_outlined);
  static const back2 = Icon(Icons.arrow_back_outlined);
  static const menu = Icon(Icons.menu);
  static const question = Icon(Icons.question_mark_sharp);
  static const person = Icon(Icons.person);
  static const play = Icon(Icons.play_circle);
  static const left = Icon(Icons.arrow_back_ios_rounded);
  static const right = Icon(Icons.arrow_forward_ios_rounded);
}

///oziga mos icon topa olmadim
